# Defaults for integration tests against hack/mq-docker (sourced by wait script only).
export KURATOR_INTEGRATION_MQ_ENDPOINT="${KURATOR_INTEGRATION_MQ_ENDPOINT:-https://127.0.0.1:9443}"
export KURATOR_INTEGRATION_MQ_QMGR="${KURATOR_INTEGRATION_MQ_QMGR:-QM1}"
export KURATOR_INTEGRATION_MQ_USER="${KURATOR_INTEGRATION_MQ_USER:-admin}"
export KURATOR_INTEGRATION_MQ_PASSWORD="${KURATOR_INTEGRATION_MQ_PASSWORD:-passw0rd}"
