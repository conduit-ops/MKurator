{{- if and .Values.metrics.enabled .Values.metrics.prometheusRule.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "kurator.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kurator.labels" . | nindent 4 }}
    {{- with .Values.metrics.prometheusRule.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  groups:
    - name: kurator.rules
      rules:
        - alert: KuratorMetricsTargetDown
          expr: |
            absent(up{namespace="{{ .Release.Namespace }}", service="{{ include "kurator.fullname" . }}-metrics"}) == 1
            or max(up{namespace="{{ .Release.Namespace }}", service="{{ include "kurator.fullname" . }}-metrics"}) == 0
          for: 10m
          labels:
            severity: critical
          annotations:
            summary: Kurator controller metrics target is down
            description: Prometheus is not scraping the Kurator metrics Service in {{ .Release.Namespace }}.
        - alert: KuratorReconcileErrors
          expr: |
            sum(increase(kurator_reconcile_errors_total[15m])) > 0
          for: 5m
          labels:
            severity: warning
          annotations:
            summary: Kurator reconcile errors detected
            description: One or more Kurator reconcilers returned errors in the last 15 minutes.
        - alert: KuratorMQOperationErrors
          expr: |
            sum(increase(kurator_mq_operations_total{result="error"}[15m])) > 0
          for: 10m
          labels:
            severity: warning
          annotations:
            summary: Kurator mqweb operation errors
            description: mqweb adapter operations are failing (check QueueManagerConnection credentials and mqweb reachability).
        - alert: KuratorOperatorNotReady
          expr: |
            max(
              kube_pod_status_ready{
                namespace="{{ .Release.Namespace }}",
                pod=~"{{ include "kurator.fullname" . }}-controller-manager-.*",
                condition="true"
              }
            ) == 0
          for: 5m
          labels:
            severity: critical
          annotations:
            summary: Kurator controller pod is not ready
            description: |
              The controller-manager pod in {{ .Release.Namespace }} is not passing its readiness probe (/readyz).
              When QueueManagerConnections exist but none are Ready, /readyz fails (REL-7) and the Deployment stops routing traffic.
        - alert: KuratorQMCPingFailures
          expr: |
            sum(increase(kurator_mq_operations_total{operation="ping",result="error"}[15m])) > 0
          for: 10m
          labels:
            severity: warning
          annotations:
            summary: Kurator QueueManagerConnection ping failures
            description: mqweb ping operations are failing (check credentials, TLS, and mqweb reachability on QueueManagerConnection resources).
        - alert: KuratorQMCReconcileErrors
          expr: |
            sum(increase(kurator_reconcile_errors_total{controller="queuemanagerconnection"}[15m])) > 0
          for: 5m
          labels:
            severity: warning
          annotations:
            summary: Kurator QueueManagerConnection reconcile errors
            description: The queuemanagerconnection reconciler returned errors in the last 15 minutes (see operator logs and QMC status conditions).
        - alert: KuratorAuthMQOperationErrors
          expr: |
            sum(increase(kurator_mq_operations_total{operation=~"set_channel_auth|get_channel_auth|delete_channel_auth|set_authority|get_authority|delete_authority",result="error"}[15m])) > 0
          for: 10m
          labels:
            severity: warning
          annotations:
            summary: Kurator auth mqweb operation errors
            description: ChannelAuthRule or AuthorityRecord mqweb operations (get/set/delete channel auth or authority) are failing.
{{- end }}
