{{- if and .Values.webhooks.enabled .Values.webhooks.certManager.create }}
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "kurator.fullname" . }}-selfsigned-issuer
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kurator.labels" . | nindent 4 }}
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kurator.fullname" . }}-serving-cert
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kurator.labels" . | nindent 4 }}
spec:
  dnsNames:
    - {{ include "kurator.fullname" . }}-webhook-service.{{ .Release.Namespace }}.svc
    - {{ include "kurator.fullname" . }}-webhook-service.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: {{ include "kurator.fullname" . }}-selfsigned-issuer
  secretName: {{ .Values.webhooks.certManager.secretName }}
{{- end }}
